server:
  port: 9998
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri:  https://gitee.com/wzv_rain/config-server.git
          search-paths: /**
      label: master